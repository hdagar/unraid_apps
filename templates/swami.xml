<?xml version="1.0"?>
<Container>
  <Name>SWAMI</Name>
  <Repository>hetain/swami:latest</Repository>
  <Network>bridge</Network>
  <Shell>bash</Shell>
  <Privileged>false</Privileged>
  <WebUI>http://[IP]:[PORT:1050]</WebUI>
  <Icon>https://raw.githubusercontent.com/hdagar/unraid_apps/main/icons/swami-icon.png</Icon>
  <Support>https://github.com/hdagar/unraid_apps/issues</Support>
  <Project>https://github.com/hdagar/unraid_apps</Project>
  <Overview>
    SWAMI is a lightweight, super-fast, browser-based catalogue for your Movies directory. 
    No media scraping, instant filters, and one-click updating. 
    Perfect for browsing on Unraid without using your media front end!
  </Overview>
  <Description>
    <br><b>What is SWAMI?</b>
    <br>
    SWAMI is a Docker-ready web UI for instant, filterable browsing of your Movies folder on Unraid (or any system).
    <ul>
      <li>Zero scraping—catalogue is built from file/folder names only.</li>
      <li>Safe: never writes or modifies your Movies share.</li>
      <li>One-click update. Dark/light mode. Filter by year, quality, keyword.</li>
    </ul>
    <br>
    <b>Quick Start:</b>
    <ol>
      <li>Set the path to your Movies folder below (do NOT put your actual Movies folder in appdata).</li>
      <li>Leave <i>SWAMI Config Folder</i> as the default (<code>/mnt/cache/docker/appdata/swami</code>).</li>
      <li>Adjust filter/label options if you wish (see field descriptions below).</li>
      <li>Open WebUI after starting the container. That's it!</li>
    </ol>
    <b>Documentation &amp; Example use:</b> See the README at <a href="https://github.com/hdagar/unraid_apps" target="_blank">https://github.com/hdagar/unraid_apps</a>
  </Description>
  <Config>
    <Path>
      <Name>SWAMI Config Folder</Name>
      <Target>/mnt/cache/docker/appdata/swami</Target>
      <Description>
        SWAMI stores its config in the <b>/data</b> folder inside the container (default is the SWAMI docker appdata folder).
        This should point to a path on your Unraid server such as <code>/mnt/cache/docker/appdata/swami</code> or <code>/mnt/user/appdata/swami</code> for persistent settings, scan files, etc.
      </Description>
      <Mode>rw</Mode>
      <ContainerDir>/data</ContainerDir>
    </Path>
    <Path>
      <Name>HOST path to Movies folder</Name>
      <Target>/mnt/user/Movies</Target>
      <Description>
        Path on your host to your Movies folder / share.<br>
        <b>READ ONLY permissions.</b>
      </Description>
      <Mode>ro</Mode>
      <ContainerDir>/mnt/user/Movies</ContainerDir>
    </Path>
    <Variable>
      <Name>MOVIE_PATH</Name>
      <Label>Movies path inside container (MOVIE_PATH)</Label>
      <Default>/mnt/user/Movies</Default>
      <Description>
        Path inside the container where your Movies folder is accessible.<br>
        <b>Default is <code>/mnt/user/Movies</code>. This must match the container path from your Movies folder mapping above.</b><br>
        Only change this if you remap your Movies folder to a different path inside the container.
      </Description>
    </Variable>
    <Variable>
      <Name>CATALOG_PATH</Name>
      <Label>SWAMI Catalogue Path (CATALOG_PATH)</Label>
      <Default>/data</Default>
      <Description>
        Where SWAMI will store its data.<br>
        <b>Must match container path value in "SWAMI Config Folder".</b>
      </Description>
    </Variable>
    <Variable>
      <Name>FILTER_LABELS</Name>
      <Label>UI Filters (comma separated)</Label>
      <Default>BluRay,UHD 4K,1080p,720p</Default>
      <Description>
        The filters you want defined. Comma-separated. <b>CAUTION:</b> Suggest a maximum of 6 for usability.<br>
        Example: <code>BluRay,UHD 4K,1080p,720p</code>
      </Description>
    </Variable>
    <Variable>
      <Name>REMOVE_LABELS</Name>
      <Label>Ignored/Filter Labels (comma separated)</Label>
      <Default>01BluRay,02UHD4K,.stfolder</Default>
      <Description>
        Comma-separated list. Anything with these as part of the file or directory name will be filtered out from appearing in the catalogue.<br>
        Example: <code>01BluRay,02UHD4K,.stfolder</code>
      </Description>
    </Variable>
    <Variable>
      <Name>FLASK_PORT</Name>
      <Label>Flask Web Server Port (FLASK_PORT)</Label>
      <Default>1050</Default>
      <Description>
        The port SWAMI's Flask web server will listen on inside the container.<br>
        <b>Default here is <code>1050</code> for Unraid. In the Dockerfile, the default is <code>5000</code>, but this will be <b>overridden</b> by this template setting.</b><br>
        If you change this, make sure the "Container Port" in the port mapping below matches. 
        <br>Unless you have a port conflict, you can leave this as <code>1050</code>.
      </Description>
    </Variable>
    <Port>
      <ContainerPort>1050</ContainerPort>
      <HostPort>1050</HostPort>
      <Protocol>tcp</Protocol>
      <Description>WebUI</Description>
    </Port>
  </Config>
</Container>
<!-- Use promote_to_public.sh to replace placeholders for public release -->